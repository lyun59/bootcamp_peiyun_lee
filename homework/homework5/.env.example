# Homework 05 — Data storage paths (template)
# Copy this file to `.env`. These control where the notebook writes raw and
# processed data, so the same code can point at different folders per machine.
DATA_DIR_RAW=data/raw
DATA_DIR_PROCESSED=data/processed
