# TSM project configuration (copy to .env and edit as needed)
# yfinance is keyless, so there are no secrets here — just reproducible settings.
TICKER=TSM
START=2020-01-01
END=2025-12-31

# Storage paths (relative to the project/ root)
DATA_DIR_RAW=data/raw
DATA_DIR_PROCESSED=data/processed
